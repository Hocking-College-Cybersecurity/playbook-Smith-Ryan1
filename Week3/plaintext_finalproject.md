Raffle number generation
lines of text describing what its purpose is

Display menu
    If you wish to choose your own number, press 1 (from table of available numbers)
    If you wish to have a random number picked for you, press 2 (again, from table of available numbers)
    if neither inputs are numbers, feed error message, loop

Menu 2
    Please enter your name (Name, non integer input)
    (feed back input), Is this correct?
    If yes, break
    if no, repeat input
    Error message if integer input, repeat input regardless

Data table
    feed data one and data two into a table, equating name to number

number selection
    choose a number from the data table
    print "you are the winner!"
    if no number is selected, nobody is the winner, house wins.